# Gradle 守护进程 JVM 参数
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX 包结构（本库不依赖 androidx，仅为宿主工程兼容保留默认配置）
android.useAndroidX=true
# 每个 module 只生成自身资源的 R 类
android.nonTransitiveRClass=true
